node_modules
dist
.env
.claude
.vscode
# Claude Code agents and configuration
.claude/
.claude/agents/
.github/agents
CLAUDE.md

# Manual patch / variant-file workflow is banned — use branches + PRs
*.patch
*.diff
*_diff.txt
*.labbai.*

# npm is not used here — pnpm-lock.yaml is the lockfile
package-lock.json
